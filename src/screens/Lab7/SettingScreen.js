import { Text, View } from "react-native"

const Set = () =>{
    return(
        <View style={{flex:1, alignItems:'center', justifyContent:'center'}}>
            <Text style={{color:'#006600', fontSize:40, fontStyle:'italic'}}>SettingScreen</Text>
        </View>
    )
}
export default Set;